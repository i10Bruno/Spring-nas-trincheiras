spring:
  application:
    name: anime-service

threads:
  virtual:
    enabled: true

server:
  servlet:
    context-path: /

  error:
    include-stacktrace: on_param

database:
  url:
  username: ${LOCAL_USERNAME}
  password: ${LOCAL_USERNAME}

